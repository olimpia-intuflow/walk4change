# SeaSteps frontend — zmienne środowiskowe
#
# Adres backendu Kamila (Rust/Axum API).
# Puste = tryb MOCK (dane demo, bez backendu) — tak działa teraz.
# Gdy backend będzie wystawiony, ustaw np.:
#   VITE_API_BASE=http://localhost:8080
#   VITE_API_BASE=https://walk4change-api.fly.dev
VITE_API_BASE=
